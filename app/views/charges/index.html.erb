<h1>Order History</h1>

<% if @orders.any? %>
  <% @orders.each do |order| %>
    <p> <%= order.order_items %> </p>
    <%= @orders.first.order_items.first.product.name %>
  <% end %>
<% else %>
  <p>You haven't made any orders yet.</p>
<% end %>
